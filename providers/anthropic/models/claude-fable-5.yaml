name: claude-fable-5

apis:
  chat_completion:
    api_format: anthropic
    endpoint: /messages
    context:
      max_input: 500000
      max_output: 128000
    features:
      tool_use: true
      reasoning: true
      structured_output: true
    parameters:
      max_tokens: 50000
